name: "Build Test"

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  build-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Download docker-compose-wait
        run: |
          wget https://github.com/ufoscout/docker-compose-wait/releases/download/2.12.1/wait
          chmod +x ./wait
          TEST_SECRET=${{ secrets.TEST_SECRET }}
          echo 1
          echo $TEST_SECRET
          echo 2
          echo "wss://mainnet.infura.io/ws/v3/$TEST_SECRET"
          echo 3
          echo "wss://mainnet.infura.io/ws/v3/${{ secrets.TEST_SECRET }}"
          echo 4
          echo "wss://mainnet.infura.io/ws/v3/${TEST_ENV}"
          echo 5
        env:
          TEST_ENV: wss://mainnet.infura.io/ws/v3/${{ secrets.TEST_SECRET }}

      - name: rustfmt
        run: |
          cargo fmt --all --check

      - name: clippy
        run: |
          cargo clippy --all-features --all-targets -- -D warnings

      - name: Start the test environment
        run: |
          docker compose up -d
          WAIT_HOSTS="localhost:5432" ./wait

      - name: test
        run: |
          cargo test
        env:
          APP__ETH_NODE__WSS_URL: wss://mainnet.infura.io/ws/v3/${{ secrets.INFURA_API_KEY }}
